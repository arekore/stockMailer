<h1>Edit</h1>
<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= form_for(@stock) do |f| %>

            <div class="form-group">
                <%= f.label '会社名' %>
                <%= f.text_field :name, class: 'form-control', placeholder: 'hogehoge株式会社' %>
            </div>

            <div class="form-group">
                <%= f.label '銘柄コード' %>
                <%= f.text_field :code, class: 'form-control', placeholder: '0000' %>
            </div>

            <div class="custom-control custom-checkbox">
                <%= f.check_box :sendFlag , class: "custom-control-input", id: "flag" %>
                <%= f.label "Activate", class: "custom-control-label", for: "flag" %>
            </div>

            <%= f.submit "Update", data: {disable_with: 'Updating...'}, class: "btn btn-primary" %>
            <%= link_to "Cancel", root_path, class: 'btn btn-danger', title: 'Return to Top' %>

        <% end %>
    </div>
</div>
